services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "3000"
    environment:
      - NODE_ENV=production
    env_file:
      - .env
    networks:
      - n8n
    restart: unless-stopped

networks:
  n8n:
    external: true